fastapi==0.115.6
uvicorn[standard]==0.34.0
psycopg[binary,pool]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
openpyxl==3.1.5
# The 2026 fixed asset schedule is a real .xls — an OLE2 compound document
# from Crystal Reports, not a renamed xlsx — and openpyxl cannot open one.
# Pure Python, so the image grows no native library.
xlrd==2.0.2
# Invoice regeneration. Pure Python, no system libraries, so the Docker
# image does not grow a native dependency for one PDF.
reportlab==4.2.5
boto3==1.35.90
pytest==8.3.4
# Reading a rendered invoice back, so the tests and the drives assert
# what a reader would see rather than what the bytes happen to contain:
# PDF text is compressed, and searching the raw bytes for a phrase that
# is plainly on the page finds nothing.
pypdf==6.18.0
httpx==0.28.1
bcrypt==4.2.1
PyJWT==2.10.1
email-validator==2.2.0
