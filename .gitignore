__pycache__/
*.pyc
.env
.venv/
venv/
storage/
node_modules/
web/dist/
.pytest_cache/
.DS_Store

# Raw client uploads — never commit. Route PII to the bucket.
/intake/

# Uploaded documents. Receipts, invoices and payroll records belong in the
# bucket, not in an image that gets copied around.
/var/

# Local tooling
.venv/
.hypothesis/
.pytest_cache/

# First-login password sheets. Never committed: these are real credentials
# for real people, and the sheet is deleted once everybody has signed in.
scripts/first_passwords.json
